# Object-Oriented JavaScript

Recommended teaching order: objects → constructor functions → prototypes → classes → constructors → `this` → instance methods → static members → getters/setters → encapsulation → private fields → inheritance → overriding → polymorphism → abstraction → composition.

Important: JavaScript classes are built on the language's prototype system.