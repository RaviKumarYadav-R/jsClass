# JavaScript Architecture

Teach separation of concerns, modules, layering, dependency inversion, dependency injection, domain/application/infrastructure boundaries, error boundaries, configuration, validation and testability.

Use small projects to show why architecture exists. Do not introduce abstraction layers before the learner understands the problem they solve.