# DOM

Teach the DOM as the browser's object representation of an HTML document. Cover selection, traversal, creation, attributes, classes, text/HTML, events, forms, event delegation, MutationObserver, ResizeObserver and IntersectionObserver.