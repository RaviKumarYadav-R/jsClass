# Fundamentals

Teach variables → types → conversion → operators → conditions → loops. The old function chapter is retained here only as a migration bridge; use `03_Functions` for the main function curriculum.