# TypeScript

Sequence: primitive types → arrays/objects → unions/intersections → narrowing → functions → interfaces/type aliases → generics → utility types → classes → modules → declaration files → strict mode → API/domain modeling.

Always connect TypeScript concepts back to the JavaScript runtime: types are primarily a development-time/static analysis layer.