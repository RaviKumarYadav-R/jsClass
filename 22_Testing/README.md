# Testing

Sequence: test vocabulary → assertions → unit tests → test doubles/mocks → integration tests → async testing → edge cases → testable architecture → coverage and its limits.

Every example should demonstrate what behavior is being protected, not merely how to make a test pass.